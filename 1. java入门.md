# 1. HelloWorld
##  开发步骤

开发一个Java程序要经历以下几个步骤

1. **编写**
	1. 编写代码即可
2. **编译**
	1. 使用 `javac 文件名`
3. **运行**
	1. 使用`java 编译过的文件名`
	2. 不要带后缀名否则运行失败

## 注意

**从JDK11开始可以直接使用`java 源文件`来执行代码 不需要javac编译**

## 编写基本要求

- 代码文件的名称，**后缀必须是`.java`结尾**
- 代码文件的名称，**必须与代码里的类名一致**

## 代码演示

**HelloWorld.java**
```java
// 这里注意 class名要和文件名保持一致
public class HelloWorld {
  public static void main(String[] args)  {
      System.out.print("hello world 你好世界");
    }
 }
```

**执行步骤**
```bash
# 1. 
javac HelloWorld.java
# 2.
java Helloworld
```

# 项目构成
- `->`: 表示包含

**工程 -> 模块 -> 包 -> 类**
- 工程：就是一个项目
- 模块：
- 包：
- 类：

# 变量

**构成**
```
类型 变量名 = 值

eg: int age = 18
```

**注意事项**
- 变量要先声明才能使用
- 变量一旦确定类型 后面只能使用该类型的值
- 变量是有作用域的，从定义到"}"截止范围内有效
- 同一个作用域内 变量名不能重名
- 变量定义的时候可以不赋初始值，但是在使用前，变量里面必须有值

# 数据类型

## 基本数据类型

**4大类8种**

| 数据类型 | 数据类型         | 内存占用(字节数) | 数据范围                                                   |
| ---- | ------------ | --------- | ------------------------------------------------------ |
| 整型   | `byte`       | 1         | -128-127                                               |
| 整型   | `short`      | 2         | -32,768 ~ 32,767                                       |
| 整型   | `int`(默认)    | 4         | -2,147,483,648 ~ 2,147,483,647                         |
| 整型   | `long`       | 8         | -9,223,372,036,854,775,808 ~ 9,223,372,036,854,775,807 |
| 浮点型  | `float`      | 4         | 约 ±1.4E-45 ~ ±3.4028235E+38                            |
| 浮点型  | `double`(默认) | 8         | 约 ±4.9E-324 ~ ±1.7976931348623157E+308                 |
| 字符型  | `char`       | 2         | 0 ~ 65,535（Unicode 码点）                                 |
| 布尔型  | `boolean`    | 1         | true 或 false                                           |
**代码**
```java
public class VariableDemo2 {
    public static void main(String[] args) {
        // byte 字节整形
        byte age = 12;
        // short 短整型
        short num1 = 123;
        // int 整型
        int num2 = 1234;
        /*
           long 长整型
           注意：随便写的整数数字 1223372036854 775807 默认是int 虽然没有超过long范围
           但是它超过了int的范围 所以报错，如果希望1223372036854775807是long那么必须后面添加L/l
         */
        long num3 = 1223372036854775807L;

        /*
           float 单精度浮点型
           1.99这种字面量小数默认的类型是double类型
           num4的类型是float 所以不能赋值
           除非把1.99后面添加一个F/f 表示是float 才能正常赋值
         */
        float num4 = 1.99F;
        // double
        double num5 = 2.11;

        //char 字符型 必须使用单引号
        char c = 'n';

        // boolean 布尔型
        boolean isShort=true;

        // 字符串类型（引用数据类型）
        String name="zs";
    }
}

```
## 自动类型转换

**类型范围小的变量，可以直接赋值给类型大的变量**

比如： byte类型可以直接赋值给int类型

**自动类型转换在计算机中的执行原理**
- a是byte 占用1个字节 也就是8位
- b是int类型 占用4个字节 也就是32位
- 所以a的范围比b的范围小可以赋值
![[Pasted image 20260428143143.png]]

**自动类型转换的其他形式**
- 箭头左侧的都可以赋值给右侧的
- long(8字节)可以赋值给float(4字节)是因为float的数值范围比long大
- char可以赋值给int
	- 从 范围看 char是2字节 int是4字节 chat比int小
	- 从类型上来看 char它存储的是 **Unicode** 字符编号 编号也是整型

```
byte -> short -> int -> long -> float -> double

char -> int
```

**代码**
```java
public class TypeConversionDemo1 {
    public static void main(String[] args) {
        byte a = 12;
        int b = a; // 这里发生了自动类型转换
        System.out.println(a); // 12
        System.out.println(b); // 12

        int i =999;
        double j = i;
        System.out.println(j); // 999
        System.out.println(i); // 999.0


        char c = 'b'; // 编码是98
        int it = c;
        System.out.println(c); // b
        System.out.println(it); // 98 这里输出因为类型是int 所以输出的是98
    }
}
```

## 表达式自动类型转换

**在表达式中，小范围类型的变量，会自动转换成表达式中较大范围的类型，再参与运行**

```
byte、short、char -> int -> long -> float -> double
```

**注意**
 - 表达式的最终结果类型由表达式中的**最高类型决定**
 - 在表达式中，byte、short、char直接转换成`int`进行运算的
**代码**
```java
public class TypeConversionDemo2 {
    public static void main(String[] args) {
        byte a = 10;
        short b = 20;
        int c = 30;
        long d = 40;
        // int result = a + b + c + d; 报错 最大类型是long
        long result = a + b + c + d;


        // long result2 = a + d + 3.14; // 报错最大类型是double 3.14的类型是double
        double result2 = a + d + 3.14;


        byte a1 = 10;
        byte a2 = 20;
        // byte a3 = a1 + a2; // 报错 byte、short、char直接转换成int进行运算的
        int a3 = a1 + a2;
    }
}

```

## 强制类型转换

- 强行将类型范围大的变量、数据赋值给类型范围小的变量
```
数据类型 变量2 = (数据类型) 变量1、数据
```

**转换原理**
- 直接从范围大的类型中获取符合小类型位数的值放进去
- 对于强制转换的位数超过的会进行截取
![[Pasted image 20260428160218.png]]

**注意**
- 强制类型转换可能会造成**数据溢出**
- 浮点型强制转换成整型，直接**丢掉小数部分**，保留整数返回


**代码**
```java
package com.puta99.type;

public class TypeConversionDemo3 {
    public static void main(String[] args) {
        int a = 20;
        // byte b = a; // 报错 大范围类型的伴侣不能直接赋值给小范围类型的变量
        byte b = (byte) a; // 强制转换

        int i = 1500;
        byte j = (byte) i; // 强制转换类型可能出现数据溢出(失真)
        System.out.println(i); // 1500
        System.out.println(j); // -36


        double score = 99.5;
        int number = (int) score; // 浮点类型小数强制转换成整数直接丢失小数部分，返回整数部分
        System.out.println(number); // 99
    }
}

```
# 运算符

## 基本运算符

```
+ - * / %
```

**注意**
- 两个整型如果相除是小数，那么会被强制转成整数
- `+`还能作为连接符，口诀：能算就算，不能算就在一起

**代码**
```java
public class OperatorDemo1 {
    public static void main(String[] args) {
        int a = 5;
        int b = 2;
        System.out.println(a + b); // 7
        System.out.println(a - b); // 3
        System.out.println(a * b); // 10
        // 因为a和b两个类型中最大的范围是int 虽然正确结果是2.5 但是会转成int 那么最终就是2
        System.out.println(a / b); // 2
        System.out.println(a % b); // 1 取余

        // a/b 如果想要得到2.5
        // System.out.println(a / b * 1.0); // 2.0 不能这样 因为a/b先执行变成2.0了
        System.out.println(1.0 * a / b); // 2.5
        
         // + 运算符
        int c = 5;
        System.out.println(c + "a"); // 5a
        System.out.println(c + 5); // 10
        System.out.println(c + 'a'+ "哈哈"); // 102哈哈  'a'是char存储的是编码97 所以可以计算结果是102
    }
}

```

## 自增、自减
- 只能操作变量 ，不能操作字面量

**单独使用**
- 单独使用++、--在变量前后效果是一样的
```java
public class OperatorDemo3 {
    public static void main(String[] args) {
        int a = 10;
        a++;
        ++a;
        System.out.println(a);

        int b = 10;
        b--;
        --b;
        System.out.println(b); // 8
    }
}
```

**非单独使用**
- ++、--如果不是单独使用(如在表达式中、或者同时有其它操作)，放在变量前、后有明显的区别

**变量前（先加后用）**
- 放在变量前面，先对变量进行+1、-1操作，在拿变量的值进行运算
```java
int a = 10;
int rs = ++a
System.out.println(rs); // 11
```
**变量后（先用后加）**
- 放在变量后面，先拿变量的值进行运算，在对变量的值进行+1、-1
```java
int a = 10;
int rs = a++
System.out.println(rs); // 10
```

## 赋值运算符
- **=**，从右边往左看
```java
// 先看=右边，把数据10赋值给左边的变量a存储
int a = 10; 
```

## 拓展赋值运算符

- 扩展的赋值运算符隐含了强制类型转换

| 符号  | 用法   | 作用    | 底层代码形式             |
| --- | ---- | ----- | ------------------ |
| +=  | a+=b | 加后赋值  | a = (a的类型)(a + b); |
| -=  | a-=b | 减后赋值  | a = (a的类型)(a - b); |
| *=  | a*=b | 乘后赋值  | a = (a的类型)(a * b); |
| /=  | a/=b | 除后赋值  | a = (a的类型)(a / b); |
| %=  | a%=b | 取余后赋值 | a = (a的类型)(a % b); |
**举例**
```java
int a = 10;
byte b = 1;
a += b;

// 等价于下面的写法
// 首先a+b的值
// 然后把它们的值强制转换成a(int)的类型
// 最后把转换后的值赋值给a
a = (int) (a + b);


// 可以避免自带强制类型转换  
byte t1 = 100;  
byte t2 = 10;  
// byte t3 = t1 + t2; 报错 因为在表达式中，byte、short、char直接转换成`int`进行运算的  
t1 += t2; // 这样就能避免转换成int了 因为等价于 t1 = (byte) (t1 + t2);
```

## 关系运算符

- java没有ja中全等于(`===`)的概念

| 符号   | 例子     | 作用       | 结果                 |
| ---- | ------ | -------- | ------------------ |
| `>`  | a > b  | 判断a是否大于b | 成立返回true，否则返回false |
| `>=` | a >= b | 大于等于     |                    |
| `<`  | a < b  | 小于       |                    |
| `<=` | a <= b | 小于等于     |                    |
| `==` | a == b | 等于       |                    |
| `!=` | a != b | 不等于      |                    |
## 逻辑运算符

- 在java中&、|，无论左边是fasle或true右边都要执行
- 由于&&、||运算效率更高、在开发中用的更多

| **符号** | **叫法** | **例子**           | **运算逻辑**                                          |
| ------ | ------ | ---------------- | ------------------------------------------------- |
| **&**  | 逻辑与    | `2 > 1 & 3 > 2`  | 多个条件必须都是 **true**，结果才是 **true**；否则是 false。        |
| **\|** | 逻辑或    | `2 > 1 \| 3 < 5` | 多个条件中只要有一个是 **true**，结果就是 **true**。               |
| **!**  | 逻辑非    | `!(2 > 1)`       | 取反                                                |
| **^**  | 逻辑异或   | `2 > 1 ^ 3 > 1`  | 前后条件的结果相同，就直接返回 **false**；前后条件的结果不同，才返回 **true**。 |
| &&     | 短路与    | 2>10&&3>2        | 判断结果同"&"，左边位fasle，右边不执行                           |
| \|\|   | 短路或    | 2>1\|\|3<5       | 判断结果同"\|"，左边位true，右边不执行                           |
## 三元运算符

```
# 三元运算
表达式 ? 值1 : 值2


# 多元运算
表达式1 ? 值1 : 表达式n ? 值2 : 值3 
```

**举例**
```java
// 三元运算
int score = 90;  
String res = score >= 90 ? "优秀" : "良好";  
System.out.println(res); // 优秀


// 多元运算
String res = score >= 90 ? "优秀" : score >= 80 ? "良好" : "及格";
```

## 运算符优先级
| **优先级**     | **运算符**                                                  |
| ----------- | -------------------------------------------------------- |
| **1 (最高)**  | 分隔符：`()`, `[]`, `.`, `(参数列表)`, `{}`                      |
| **2**       | 一元运算符：`++`, `--`, `+` (正), `-` (负), `~` (位取反), `!` (逻辑非) |
| **3**       | 乘法/除法/取余：`*`, `/`, `%`                                   |
| **4**       | 加法/减法：`+`, `-`                                           |
| **5**       | 移位运算符：`<<`, `>>`, `>>>`                                  |
| **6**       | 关系运算符：`<`, `<=`, `>`, `>=`, `instanceof`                 |
| **7**       | 相等运算符：`==`, `!=`                                         |
| **8**       | 按位与：`&`                                                  |
| **9**       | 按位异或：`^`                                                 |
| **10**      | 按位或：**\|**                                               |
| **11**      | 短路与：`&&`                                                 |
| **12**      | 短路或：**\|\|**                                             |
| **13**      | 三目运算符：`? :`                                              |
| **14 (最低)** | 赋值运算符：`=`, `+=`, `-=`, `*=`, `/=`, `%=`, `&=`, `^=`,     |
